name: module3_task2
on:
  schedule:
    - cron: "2 0 * * *"
  push:
jobs:
  deploy:
    runs-on: ubuntu-18.04
    steps:
      - name: Clone the Repo
        uses: actions/checkout@v3
      - working-directory: module3_task2
        run: ./setup.sh
      - working-directory: module3_task2
        run: make help
      - name: Build the server
        working-directory: module3_task2
        run: make build
      - name: Create a post
        working-directory: module3_task2
        run: make POST_NAME=welcome_again POST_TITLE="Welcome again" post
      - working-directory: module3_task2
        run: make package
      - run: echo "artifactPath=module3_task2/.github/workflows/module3_task2.yml" >> $GITHUB_ENV
      - uses: actions/upload-artifact@v3
	env:
          name: my-artifact
          path: ${{ env.artifactPath }}
